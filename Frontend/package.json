{
  "name": "lastmile-carbon",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm --prefix apps/web run build",
    "dev:web": "npm --prefix apps/web run dev",
    "dev:rider": "npm --prefix apps/rider run dev",
    "build:web": "npm --prefix apps/web run build",
    "build:rider": "npm --prefix apps/rider run build"
  }
}
